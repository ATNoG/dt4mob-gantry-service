## Further Help

- Check `config.toml` against `config.example.toml` for field reference
- Enable `DEBUG` or `TRACE` logging to see detailed processing output
- Review the [Developer Guide](../DEVELOPER.md) for architecture details
